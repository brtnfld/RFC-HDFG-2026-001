\revision{v1}{March 6, 2026}{Initial draft released for internal review.}
\revision{v2}{May 1, 2026}{Replaced \texttt{H5Pset\_filter2} with \texttt{H5Pappend\_filter} accepting a string-encoded filter configuration (\texttt{H5Z\_params\_t} is an internal struct; callers use convenience macros (C) or overloaded methods (C++) and never interact with it directly); dropped \texttt{name} field and Filter Name Registry in favour of \texttt{filter\_title} packed into \texttt{cd\_values}; deferred \texttt{H5O\_PLINE\_VERSION\_3} on-disk format extension to a follow-on RFC; added single-quoted value production (\texttt{squoted-value}) to the grammar; added C++ (\texttt{appendFilter} overloads), Fortran (BIND(C) + C helper), Java, and h5py/HLL binding designs; documented \texttt{H5Pmodify\_filter} gap and CB-08 modify-filter test pattern; addressed C++ macro portability, legacy plugin \texttt{cd\_nelmts} hazard, \texttt{int64\_t} pack helpers, and VOL thin-client limitation.}
\revision{v3}{May 12, 2026}{Added TOML VFD/VOL profile and use-case coverage (UC-1--UC-8) as non-normative forward-looking context; VFD/VOL API changes are out of scope. Community review: \texttt{true}/\texttt{false} are the only valid boolean literals; empty quoted strings (\texttt{key=""}) are valid. Clarified \texttt{H5Z\_CLASS\_T\_VERS\_MAX} versioning. Corrected SZIP \texttt{set\_config} (validates directly, does not delegate to \texttt{H5Pset\_szip}) and Fortran helper code (field-by-field struct construction).}
