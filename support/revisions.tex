\revision{v1}{March 6, 2026}{Initial draft released for internal review.}
\revision{v2}{May 1, 2026}{Renamed to \texttt{H5Pappend\_filter}; \texttt{H5Z\_params\_t} made internal with convenience macros for callers. Dropped \texttt{name} field and Filter Name Registry in favour of \texttt{filter\_title} in \texttt{cd\_values}. Deferred on-disk format change. Added single-quoted string grammar production. Added Fortran, Java, and h5py binding designs. Documented \texttt{H5Pmodify\_filter} gap; addressed legacy plugin \texttt{cd\_nelmts} hazard and VOL thin-client limitation.}
\revision{v3--v5}{May 12 -- July 15, 2026}{Community review: boolean and empty-string grammar clarifications; SZIP and Fortran corrections. Added the TOML VFD/VOL profile as non-normative forward-looking context. In-file blob storage split out into RFC-HDFG-2026-003. Adopted the on-disk format change: the parameter string is persisted in filter-pipeline version~3.}
\revision{v6--v9}{August 18, 2026}{Settled the version-3 layout: entries end in typed, length-delimited extension blocks with a critical bit; added \texttt{H5Pmodify\_filter\_by\_idx} for index-addressed edits that preserve the stored string; pinned the libver gate to \texttt{H5F\_LIBVER\_V300}; corrected the field order and extension-block numbering.}
\revision{v10--v12}{August 21--22, 2026}{Review fixes: \texttt{cd\_values\_size} is an element count; VFD/VOL material moved to Appendix~A; Fortran, C++, and Java binding gaps closed. Canonical-form corrections: the stored string is canonical rather than verbatim, canonicalisation is a fixed point, correctly-rounded decimal conversion is normative, the float form is \texttt{\%.16e}, and the accepted language is stated exactly.}
