\revision{v1}{March 6, 2026}{Initial draft released for internal review.}
\revision{v2}{April 28, 2026}{Replaced \texttt{H5Pset\_filter2} with \texttt{H5Pset\_filter\_params} accepting a tagged union \texttt{H5Z\_params\_t} (string or raw \texttt{cd\_values}); dropped \texttt{name} field and Filter Name Registry in favour of \texttt{filter\_title} packed into \texttt{cd\_values}; deferred \texttt{H5O\_PLINE\_VERSION\_3} on-disk format extension to a follow-on RFC; added C++, Fortran, and Java binding designs; addressed C++ macro portability, legacy plugin \texttt{cd\_nelmts} hazard, \texttt{int64\_t} pack helpers, and VOL thin-client limitation.}
